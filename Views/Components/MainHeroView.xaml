<?xml version="1.0" encoding="utf-8"?>

<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:strings="clr-namespace:UltimateVideoBrowser.Resources.Strings"
             x:Class="UltimateVideoBrowser.Views.Components.MainHeroView">
    <Grid Padding="16,12" BackgroundColor="{DynamicResource AppBg}">
        <Frame CornerRadius="20"
               Padding="16"
               BorderColor="{DynamicResource BorderSubtle}">
            <Frame.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="{DynamicResource Surface2}" Offset="0.0" />
                    <GradientStop Color="{DynamicResource AccentGlow}" Offset="0.6" />
                    <GradientStop Color="{DynamicResource Surface3}" Offset="1.0" />
                </LinearGradientBrush>
            </Frame.Background>

            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                <VerticalStackLayout Spacing="8">
                    <Label Text="{x:Static strings:AppResources.HeroEyebrow}"
                           FontSize="12"
                           TextColor="{DynamicResource OnSurfaceMuted}" />
                    <Label Text="{x:Static strings:AppResources.HeroTitle}"
                           FontSize="22"
                           FontAttributes="Bold"
                           TextColor="{DynamicResource OnSurface}" />
                    <Label Text="{x:Static strings:AppResources.HeroSubtitle}"
                           FontSize="13"
                           TextColor="{DynamicResource OnSurfaceMuted}" />

                    <Button Text="{x:Static strings:AppResources.ActionsButton}">
                        <Button.ContextFlyout>
                            <MenuFlyout>
                                <MenuFlyoutItem Text="{x:Static strings:AppResources.SourcesButton}"
                                                Command="{Binding OpenSourcesCommand}" />
                                <MenuFlyoutItem Text="{x:Static strings:AppResources.AlbumsButton}"
                                                Command="{Binding OpenAlbumsCommand}" />
                                <MenuFlyoutItem Text="{x:Static strings:AppResources.SettingsButton}"
                                                Command="{Binding OpenSettingsCommand}" />
                                <MenuFlyoutItem Text="{x:Static strings:AppResources.ReindexButton}"
                                                Command="{Binding RunIndexCommand}" />
                            </MenuFlyout>
                        </Button.ContextFlyout>
                    </Button>
                </VerticalStackLayout>

                <Image Grid.Column="1"
                       Source="hero_video.svg"
                       HeightRequest="120"
                       WidthRequest="140"
                       HorizontalOptions="End"
                       VerticalOptions="Center" />
            </Grid>
        </Frame>
    </Grid>
</ContentView>
