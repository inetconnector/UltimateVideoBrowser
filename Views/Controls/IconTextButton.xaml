<?xml version="1.0" encoding="utf-8" ?>

<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="UltimateVideoBrowser.Views.Controls.IconTextButton"
             x:Name="Root">
    <Frame Padding="{Binding Padding, Source={x:Reference Root}}"
           BackgroundColor="{Binding BackgroundColor, Source={x:Reference Root}}"
           BorderColor="{Binding BorderColor, Source={x:Reference Root}}"
           CornerRadius="12"
           HasShadow="False">
        <Grid ColumnDefinitions="Auto,*"
              ColumnSpacing="8"
              VerticalOptions="Center">
            <Image Source="{Binding ImageSource, Source={x:Reference Root}}"
                   HeightRequest="18"
                   WidthRequest="18"
                   VerticalOptions="Center" />
            <Label Grid.Column="1"
                   Text="{Binding Text, Source={x:Reference Root}}"
                   TextColor="{Binding TextColor, Source={x:Reference Root}}"
                   FontSize="{Binding FontSize, Source={x:Reference Root}}"
                   VerticalOptions="Center" />
        </Grid>
        <Frame.GestureRecognizers>
            <TapGestureRecognizer Command="{Binding Command, Source={x:Reference Root}}"
                                  CommandParameter="{Binding CommandParameter, Source={x:Reference Root}}" />
        </Frame.GestureRecognizers>
    </Frame>
</ContentView>
